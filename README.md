🤖 Telegram Bot + Whisper.cpp + OCR (Tesseract)

Полностью локальный Telegram-бот с поддержкой:

✅ Распознавания голосовых сообщений (Whisper.cpp)
✅ OCR для фотографий кода (Tesseract + OpenCV)
✅ Автоматической очистки временных файлов
✅ Уведомлений администратора об ошибках
✅ Очереди задач с защитой от переполнения
✅ Health checks для всех сервисов
✅ TypeScript с полной типизацией
📋 Требования

    Docker 20.10+
    Docker Compose 2.0+
    2GB RAM минимум
    Telegram Bot Token (от @BotFather)
    Telegram User ID (от @userinfobot)

🚀 Быстрый старт
1. Клонируйте репозиторий
bash

git clone <your-repo>
cd tg-voice-ocr-bot

2. Настройте переменные окружения
bash

cp .env.example .env
nano .env  # или любой другой редактор

Заполните .env:
env

BOT_TOKEN=your_bot_token_from_botfather
ADMIN_ID=your_telegram_user_id

3. Скачайте модель Whisper
bash

cd whisper
mkdir -p models
cd models
wget https://huggingface.co/ggerganov/whisper.cpp/resolve/main/ggml-base.bin
cd ../..

4. Запустите проект
bash

# Используя Make (рекомендуется)
make build
make up

# Или напрямую через Docker Compose
docker compose build
docker compose up -d

5. Проверьте статус
bash

make test
# или
curl http://localhost:8000/health  # Whisper
curl http://localhost:9001/health  # OCR

📁 Структура проекта

tg-voice-ocr-bot/
├── bot/                    # Telegram Bot (Grammy + TypeScript)
│   ├── index.ts           # Основной код бота
│   ├── package.json       # Зависимости Node.js
│   ├── tsconfig.json      # Конфигурация TypeScript
│   ├── Dockerfile         # Multi-stage build
│   └── .env              # Конфигурация (не в git)
│
├── whisper/               # Whisper.cpp HTTP сервер
│   ├── server.py         # Flask API для STT
│   ├── requirements.txt  # Python зависимости
│   ├── Dockerfile        # Сборка whisper.cpp
│   └── models/           # Модели (*.bin)
│
├── ocr/                   # OCR сервер (Tesseract + OpenCV)
│   ├── server.py         # Flask API для OCR
│   └── Dockerfile        # Установка Tesseract
│
├── docker-compose.yml     # Оркестрация сервисов
├── Makefile              # Удобные команды
├── .env.example          # Пример конфигурации
└── README.md             # Эта документация

🎯 Функциональность
🎤 Голосовые сообщения

Отправьте голосовое сообщение → получите текстовую расшифровку

Технологии:

    Whisper.cpp (локальный STT)
    FFmpeg для конвертации OGG → WAV
    Автоматическая очистка временных файлов

🖼 OCR для фотографий кода

Отправьте фото с кодом → получите текст

Обработка:

    OpenCV: grayscale → denoise → CLAHE → adaptive threshold
    Tesseract: распознавание (eng + rus)
    Pygments: определение языка программирования
    Постобработка: исправление типичных ошибок OCR

Форматы вывода:

    Короткий текст (≤4000 символов) → форматированный блок в чате
    Длинный текст → файл с расширением по типу кода

📢 Команда "следующий"

Отправьте текст следующий → получите жирный баннер:

========== СЛЕДУЮЩЕЕ ВИДЕО ==========

⚙️ Конфигурация
Переменные окружения

Переменная	Описание	Пример
BOT_TOKEN	Токен Telegram бота	1234567890:ABCdef...
ADMIN_ID	ID администратора для уведомлений	123456789
WHISPER_URL	URL Whisper API (внутренний)	http://whisper:8000/stt
OCR_URL	URL OCR API (внутренний)	http://ocr:9001/ocr

Ресурсы контейнеров

docker-compose.yml:
yaml

whisper:
  resources:
    limits: { memory: 2G }
    reservations: { memory: 512M }

ocr:
  resources:
    limits: { memory: 1G }
    reservations: { memory: 256M }

bot:
  resources:
    limits: { memory: 512M }
    reservations: { memory: 128M }

🧹 Автоматическая очистка
Bot (Node.js)

    Cron: каждые 15 минут
    Удаляет: файлы старше 30 минут из ./tmp
    Библиотека: node-cron

Whisper & OCR (Python)

    Scheduler: APScheduler каждые 10 минут
    Удаляет: файлы старше 30 минут из /tmp
    Уведомления: при ошибках очистки

🔔 Уведомления администратору

Бот отправляет сообщения админу при:

    ⚠️ Ошибках Whisper API
    ⚠️ Ошибках OCR сервиса
    ⚠️ Переполнении очереди задач
    ⚠️ Ошибках загрузки/скачивания файлов
    ⚠️ Ошибках cron очистки
    ⚠️ Недоступности сервисов (health check)

Формат:

⚠️ Ошибка:
<детали ошибки>

🛠 Makefile команды
bash

make help          # Показать все команды
make build         # Собрать контейнеры
make up            # Запустить сервисы
make down          # Остановить сервисы
make restart       # Перезапустить
make logs          # Показать логи всех сервисов
make logs-bot      # Логи только бота
make clean         # Удалить всё (контейнеры, volumes, images)
make test          # Проверить health сервисов
make rebuild       # Пересобрать и перезапустить
make status        # Статус контейнеров

🧪 Тестирование
1. Голосовое сообщение

Отправьте голосовое → получите текст
2. Фото с кодом

Отправьте скриншот кода → получите:

    Форматированный текст (если короткий)
    Файл code_<timestamp>.<ext> (если длинный)

3. Фото без текста

Отправьте пустое фото → ❗ Текст не найден на изображении.
4. Команда

Отправьте следующий → баннер
5. Переполнение очереди

Отправьте 6+ голосовых подряд → ⚠️ Очередь переполнена, попробуйте позже.
🐛 Отладка
Проверка логов
bash

# Все сервисы
docker compose logs -f

# Только бот
docker compose logs -f bot

# Whisper
docker compose logs -f whisper

# OCR
docker compose logs -f ocr

Проверка health
bash

curl http://localhost:8000/health
# {"status":"ok","model":"ggml-base.bin","tmp_files":0}

curl http://localhost:9001/health
# {"status":"ok","tesseract_version":"5.x.x","tmp_files":0}

Вход в контейнер
bash

make shell-bot      # Открыть shell в боте
make shell-whisper  # Whisper
make shell-ocr      # OCR

📊 Архитектура

┌─────────────┐
│   Telegram  │
│    Users    │
└──────┬──────┘
       │
       ▼
┌─────────────────┐      ┌──────────────┐
│   Bot Service   │─────▶│   Whisper    │
│   (Grammy TS)   │      │  (Flask API) │
└────────┬────────┘      └──────────────┘
         │
         │               ┌──────────────┐
         └──────────────▶│     OCR      │
                        │  (Flask API) │
                        └──────────────┘

Сеть: Все сервисы в одной Docker bridge сети bot_network

Volumes: Раздельные для каждого сервиса (bot_tmp, whisper_tmp, ocr_tmp)
🚨 Известные проблемы и решения
Whisper возвращает пустой текст

Причина: Плохое качество аудио или неподдерживаемый формат
Решение: Проверьте логи, попробуйте другую модель (ggml-medium.bin)
OCR не распознает текст

Причина: Низкое качество фото, сложный фон
Решение: Улучшите освещение, сделайте фото под углом 90°
Контейнер падает с OOM

Причина: Недостаточно памяти
Решение: Увеличьте лимиты в docker-compose.yml
Bot не отвечает

Причина: Неверный токен или проблемы с сетью
Решение: Проверьте .env, перезапустите: make restart
🔐 Безопасность

    Не коммитьте .env в git
    Используйте strong tokens от BotFather
    Ограничьте доступ к серверу (firewall)
    Регулярно обновляйте зависимости:

bash

   cd bot && npm update
   pip install --upgrade -r requirements.txt

📝 TODO

    Поддержка большего количества языков OCR
    Web UI для мониторинга
    Метрики (Prometheus/Grafana)
    Поддержка документов (PDF → текст)
    CI/CD pipeline
    Unit тесты
    Rate limiting для защиты от спама

📄 Лицензия

MIT License - используйте свободно
🤝 Contributing

    Fork проект
    Создайте feature branch (git checkout -b feature/AmazingFeature)
    Commit изменения (git commit -m 'Add AmazingFeature')
    Push в branch (git push origin feature/AmazingFeature)
    Откройте Pull Request

💬 Поддержка

Нашли баг? Есть предложение? Создайте issue

Сделано с ❤️ для локального AI
